namespace Concurrent;

public static class Math
{
    /**
     * For the sake of making a unit test required by the task.
     */
    public static int AddTwoNumbers(int a, int b)
    {
        return a + b;
    } 
}